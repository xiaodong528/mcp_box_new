# =============================================================================
# Memo 生产环境配置文件模板
# =============================================================================
# 使用说明：
#   1. 复制此文件为 .env:  cp .env.prod.example .env
#   2. 根据实际环境修改配置项
#   3. 注意：不要将 .env 文件提交到版本控制系统
#
# 架构说明：
#   - 本项目镜像已针对 x86_64/amd64 架构构建
#   - 适用于标准的 x86 生产服务器
#   - 如需在 ARM 机器上构建，构建脚本会自动进行跨架构编译
# =============================================================================

# =============================================================================
# E2B 沙箱配置（可选）
# =============================================================================
# 如果需要在沙箱环境中执行 MCP 工具，请配置以下参数
# 如果不使用沙箱功能，可以留空或注释掉

# E2B Jupyter 服务器地址（沙箱主机）
E2B_JUPYTER_HOST=

# E2B Jupyter 服务器端口
E2B_JUPYTER_PORT=49999

# E2B 调试模式（true/false）
E2B_DEBUG=false

# E2B API 密钥
# 获取地址: https://e2b.dev/
# 示例: e2b_833bb39cd9cb0d20dd4c13638af22864531d652c
E2B_API_KEY=

# =============================================================================
# 数据库配置（可选）
# =============================================================================
# MCP Box 可以使用 PostgreSQL 存储工具定义
# 如果使用文件存储模式（STORE_IN_FILE=true），可以忽略此部分

# 数据库主机地址
DB_HOST=

# 数据库端口
DB_PORT=5432

# 数据库名称
DB_NAME=mcpbox

# 数据库用户名
DB_USER=mcpbox

# 数据库密码
DB_PASSWORD=

# =============================================================================
# 其他配置
# =============================================================================
# 这些配置项已在 docker-compose.prod.yml 中设置，通常不需要修改

# Memo API 地址（容器内网络，无需修改）
# MEMO_API_URL=http://memo:48000

# Memo 数据库路径（容器内路径，无需修改）
# MEMO_DB_PATH=/app/data/memo.db

# MCP Box 存储模式（true=文件存储，false=数据库存储）
# STORE_IN_FILE=true
